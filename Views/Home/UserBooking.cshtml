@* @model IEnumerable<BookingVehicle> *@
@using System.Data;
@model System.Data.DataTable;
@{
  ViewData["Title"] = "View your Bookings";
  @* Layout = "_Common"; *@
}
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
<link ref="stylesheet" href="~\css\userbookcs.css"></script>

<body>
    <br>
    <br>
    <div class="boxfunc">
        <table style="width:100%" border=2>
            <br>
                    <h3> Employee Bookings</h3>
                    <thead>
                        <tr>
                            <th> EmployeeID &emsp; </th>
                            <th> PickUp&emsp; </th>
                            <th> Date &emsp; </th>
                            <th> Time &emsp; </th>
                            <th> Destination </th>
                            <th> BusNumber </th>
                            

                        </tr>
                    </thead>
                    <tbody>
                        @{

                            foreach (DataRow datarow in Model.Rows)
                            {
                                
                                <tr>
                                    <td>@datarow[0] &emsp; </td>
                                    <td>@datarow[1] &emsp; </td>
                                    <td>@datarow[3] &emsp; </td>
                                    <td>@datarow[4] &emsp; </td>
                                    <td>@datarow[2] &emsp; </td>                                   
                                    @* <td>@datarow[5] &emsp; </td>
                                    <td>@datarow[6] &emsp; </td> *@
                                    @* @{
                                        string imageBase64Data=Convert.ToBase64String((byte[])datarow[1]);
                                        string imageDataURL=string.Format("data:image/jpg;base64,{0}", imageBase64Data);
                                        <td><img src=@imageDataURL width="25px" height="25px"></td>
                                    } *@
                                    
                                    
                                    <td><div>
                                        @* <a  href="@Url.Action("DeleteProduct", "Employee", new { deleteItem = "Pending" })">*@ 
                                            @* <button style="color:green"> Approve </button>  *@
                                        @* <a  href="@Url.Action("DeleteProduct", "Employee", new { deleteItem = "Pending" })">*@ 
                                            @* <button style="color:red"> Reject </button>  *@
                                    </div></td>
                                </tr>
                            }
    
                        }
                        </tbody>
                        
                </table>            
    </div>
    <br>
    <div class="container">
        <a button asp-controller="Home" asp-action="Index" type="submit">Go back</button></a>    
    </div>

    
</body>